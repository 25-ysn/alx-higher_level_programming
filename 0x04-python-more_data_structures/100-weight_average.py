#!/usr/bin/python3
def weight_average(my_list=[]):
    if not my_list:
        return 0
    number = 0
    val = 0
    for x in my_list:
        number += tup[0] * tup[1]
        dval += tup[1]
    return (number / val)
